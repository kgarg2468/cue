# Redaction note (2026-08-31)

`app.png` originally captured the full desktop as launch evidence. Before the
repository went public it was replaced with a crop of the CaptureDelegateApp
window only, and the original blob was purged from git history. The textual
evidence in `app-observation.txt` (visible=true, backgroundOnly=false,
windows=1) is unchanged and remains the authoritative record for S-M0 runtime
verification.
