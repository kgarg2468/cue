M1 backend-shutdown cleanup slice verification — 2026-07-18

Slice: SIGTERM/SIGINT now trigger coordinated shutdown via an
async-signal-safe self-pipe handler and a dedicated shutdown thread:
every active run's cancel flag is set (ActiveRuns::cancel_all), the
registry is drained with a 5s bound (wait_until_empty, 10ms polls,
supervision loops SIGKILL each run's process group as usual), the socket
file is removed only if it is still the one this backend bound
(SocketIdentity comparison), and the backend exits 0.

Provenance: implemented by a Codex gpt-5.6-sol worker (high effort)
within a two-file allowlist (backend/src/lib.rs,
backend/tests/process_lifecycle.rs); diff gate clean. The worker's
sandbox blocked Unix-socket binding, so all integration verification ran
in the workspace.

TDD red evidence: with the two signal tests present and lib.rs reverted,
both failed at "backend should exit successfully" (signal-default
termination, no cleanup). Green after restoring the implementation.

Evidence in this directory:
- cargo-test.log — full backend suite green: 9 lib + 13 + 19 integration
  (17 prior + sigterm_kills_active_run_groups_and_removes_socket +
  sigint_kills_active_run_groups). Signal tests 8/8 stress reruns.
- verify-m0-parity.log — full CI parity gate green including Swift tests
  and health handshake.
- runtime-demo.log — release binary: run started, grandchild pid
  observed, SIGTERM sent; grandchild (with ~30s of sleep remaining) dead
  0.03s after the signal, backend exited 0 0.05s after, socket file
  removed.

Known environmental note (unchanged): the SUN_LEN socket-path library
test fails in long-path checkouts; passes here and in CI.
